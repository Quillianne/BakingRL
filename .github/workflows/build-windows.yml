name: Build BakingRL Windows

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: bakingrl-windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 120

    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Rust stable MSVC
        run: |
          rustup toolchain install stable-msvc
          rustup default stable-msvc
          rustup target add x86_64-pc-windows-msvc
          rustup show

      - name: Install frontend dependencies
        run: npm ci

      - name: Check frontend
        run: npm run check

      - name: Build frontend
        run: npm run build

      - name: Prepare Node sidecar
        run: npm run node:prepare

      - name: Smoke prepared plugin runtime
        run: >-
          npm run extension-host:smoke --
          --node src-tauri/bin/node-x86_64-pc-windows-msvc.exe
          --bootstrap src-tauri/gen/extension-host/bootstrap.mjs

      - name: Check Rust
        run: cargo check --manifest-path src-tauri/Cargo.toml

      - name: Test Rust
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib

      - name: Build Tauri app
        run: npm run tauri build

      - name: Smoke final plugin runtime inputs
        run: >-
          npm run extension-host:smoke --
          --node src-tauri/bin/node-x86_64-pc-windows-msvc.exe
          --bootstrap src-tauri/gen/extension-host/bootstrap.mjs

      - name: Upload Windows bundles
        uses: actions/upload-artifact@v7
        with:
          name: BakingRL-windows-${{ github.ref_name }}
          path: src-tauri/target/release/bundle/**
          if-no-files-found: error
          retention-days: 14
