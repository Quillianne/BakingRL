{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay-ingame", "overlay-editor", "page-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "deep-link:default",
    "dialog:default",
    "opener:default"
  ]
}
